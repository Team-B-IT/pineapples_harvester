import tensorflow as tf
import os
# os['TF_CPP_MIN_LOG_LEVEL'] = '2'

# sess = tf.Session()

# a = tf.placeholder(tf.float16, name="A")
# b = tf.placeholder(tf.float16, name="B")

# add = tf.add(a,b,name="Addition")
# result = sess.run(a, {a:32,b:44})

# print(result)

# file_writer = tf.summary.FileWriter('./logs',sess.graph)
# sess.close()